// IMPORTANT: after you deploy the backend on Render, replace the URL below
// with your actual Render service URL, e.g. 'https://movie-rating-backend.onrender.com/api'
// Then rebuild the frontend (ng build) and redeploy it.
export const environment = {
  production: true,
  apiUrl: 'https://movie-rating-backend-3.onrender.com/api'
};
